services:
  kyurie-fe:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kyurie-fe
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=http://localhost:8080
    restart: unless-stopped